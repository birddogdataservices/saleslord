{
  "name": "saleslord",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=@saleslord/web",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5"
  }
}
